import { selectCommonCode } from "@/store/selectors"
import { useSelector } from "react-redux"

function main() {
  // const code = useSelector(selectCommonCode)
  // console.log(code)
  return <div>main</div>
}

export default main
